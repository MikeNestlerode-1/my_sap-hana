parameters:
  osVersion: ''
  osImage: ''

jobs:
- job: createAllNew${{parameters.osVersion}}
  variables:
    scenario: v2-allNew-${{parameters.osVersion}}
    testcase: $(scenario)-$(Build.BuildId)
  steps:
  - template: update-place-holder.yaml
    parameters:
      scenario: $(scenario)
      testCaseName: $(testcase)
      placeHolder: var-rg-name
      value: $(testcase)
  - template: terraform-deployment-steps.yaml
    parameters:
      testCaseName: $(testcase)
      osImage: ${{parameters.osImage}}
  - template: ansible-deployment-steps.yaml
    parameters:
      testCaseName: $(testcase)
